# 变更记录 · CHANGELOG

## 2026-06-29 — 练习/考试新增「乱序」

- 答题页（练习 & 考试通用）顶部加「乱序」开关：开启后题目随机打乱顺序（Fisher–Yates），关闭恢复原顺序。
- 答案按题目 `id` 记录，乱序不丢已答；切换时保持当前题可见；乱序状态随进度持久化（刷新/恢复仍是同一乱序序列）。
- 实现于共享引擎 `mountQuiz`，离线版自动同步生效。

## 2026-06-29 — 背题模式 + 显示大小可调

- **背题模式**：题库卡新增「背题」入口（路由 `#/recite/<id>`），整页列出全部题目，答案直接标出（选项高亮正确项 + ✓ / 填空显示标准答案），解析默认折叠、点开查看；顶部可一键「展开/折叠全部解析」。舒适阅读密度，配合显示大小可一屏看更多。
- **显示大小可调**：顶栏新增 `Aa` 按钮，弹窗内 −/＋ 微调或紧凑/标准/宽松三档，全局缩放所有界面（`body { zoom }` + CSS 变量 `--ui-zoom`），记忆在 `localStorage('exam-practice:density')`，首屏预上色避免闪烁。深浅色、手机/桌面均验证。
  - 抽屉高度由 `100dvh` 改为 `top/bottom:0`，避免缩放时溢出。

## 2026-06-29 — 前后端分离 + 账号登录 + 服务器题库

参考 `learning-tracker-pwa` 的 Flask 后端思路，把项目改造成可跑在阿里云上的前后端方案：

- **后端 `backend/app.py`（Flask）**：账号密码登录（写死在 `USERS`，token 30 天有效）；题库 = `backend/banks/` 文件夹里的 `.json` 文件（与现有 `data/*.json` 格式完全一致），manifest 由后端扫描文件夹自动生成；登录后可经 API 增删题库（写/删文件）。同时托管前端 `index.html`（同源免跨域），也开了 CORS。`backend/README.md` 给了 systemd / Nginx / 占位符部署说明。
- **前端登录闸门**：未登录只见登录页，进不去首页、答题、离线下载。`localStorage` 存 token / 用户名，401 自动登出。
- **题库改走 API**：`loadManifest` / `loadExam` 经统一 `api` 层；首页直接用后端给的题数（不再逐套拉取）。
- **题库管理页 `#/manage`**：列出题库、删除、粘贴 JSON 或导入 `.json` 文件新增/覆盖。
- **本地演示模式 `DEMO_MODE`**：无后端时用内置账号（`student / exam2026`）登录、读本地 `data/`、增删写浏览器，方便本地预览。部署时改 `DEMO_MODE=false` + `API_BASE`。
- **离线下载保留**：导出仍是自包含单文件，登录后即可下载，离线全功能。

> 注：原「单文件 GitHub Pages」约束在本方案下转为「前端单文件 + 独立 Flask 后端」；前端本身仍是零依赖单文件，只是把 `fetch` 指向 API。

## 2026-06-29 — rebase 线上仓库 + 收尾修复

基于已部署仓库（含「军事理论 复习题集」398 题、`SKILL.md`、更新后的 manifest/docs）rebase。部署版 `index.html` 是较早版本，补回三处此前做过、但未随部署生效的修复：

- **判断题（`judge`）支持**：rebase 时被回退，已重新加回。题库无需写 `options`，引擎自动生成「✓ 正确 / ✕ 错误」单选；`answer:[0]`=正确、`[1]`=错误。判分 / 反馈 / 结果 / 键盘 / 导航全链路验证通过。
- **题目导航格子**：未答状态 class 由 `empty` 改为 `unans`，消除与全局 `.empty{padding:60px}` 占位规则碰撞——所有格子恢复整齐 48px 方块（398 题已验证）。
- **重新作答**：补上 `data-retry` 点击事件绑定（此前只有按钮没有处理器），点击清空本题判定 / 解析 / 已选答案并解锁重答。

同步：`SKILL.md` 登记 judge 题型与校验规则；`demo.json` 补两道判断题（共 8 题）；manifest 标题改为「四种题型」。

## 2026-06-28（二）— 配色升级 + 体验打磨

### 配色（采用方案 C · 现代石墨 + 翠绿）
- 全站换为中性灰白 + 清亮翠绿（emerald）主色，阴影中性化；深浅色都更清晰醒神。
- 新增背景纹理 token `--dot`（极淡网点）+ `--glow`（顶部柔光），克制不花哨。
- 品牌标与 favicon 换成清单勾选图标（替掉「题」字占位）。

### 交互
- 答错后新增「重新作答 ↻」：练习模式可重选并重新确认，提交按更正后答案计分。
- 横屏 / 全尺寸下「上一题 / 下一题」改为 48px 方形大按钮，触控更舒服。

### 修复
- 题目导航网格：固定 5 列、强制正方形单元格、消除横向溢出与「堆叠 bug」观感（`align-content/align-self:start` + `overflow-x:hidden`）。

---

## 2026-06-28（一）— 完整重构 + 设计系统化

### 设计系统
- 新增 `docs/design-system.md`：颜色 / 字体 / 间距 / 组件 / 交互 / 响应式的唯一规范。
- 新增 `CLAUDE.md`：项目硬性约束（单文件可部署、变量化样式、单引擎、深浅色、多端）。
- 刷新 `README.md`：补全新功能说明与项目结构。

### 视觉
- 全新护眼配色（首版为暖纸白 + 静谧绿，后于「二」升级为石墨 + 翠绿）。
- 颜色全部 token 化（`:root` + `[data-theme="dark"]`），右上角**手动切换深浅色**，记忆选择、首次跟随系统、预涂防闪烁。
- 重做首页套题卡、题卡、成绩页（环形正确率仪表 + 评级）。

### 交互
- **题目导航抽屉**：题号网格总览（未答 / 已选 / 对 / 错配色），跳题、跳到标记、直接交卷。
- **键盘快捷键**：`1–9` 选项、`←/→` 翻题、`Enter` 确认/下一题、`F` 标记、`R` 显答案、`Esc` 关导航。
- **触控手势**：题卡左右滑动切题。
- **单题标记**（`⚑`）。
- **进度自动保存**：刷新 / 退出后自动恢复，可从头开始。
- 主按钮状态机：确认 → 下一题 → 提交；未答交卷二次确认。

### 适配
- 手机竖屏 / 平板 / 桌面 / 横屏统一响应式；吸顶 header + 吸底操作栏；安全区适配；热区 ≥44px；输入框 ≥16px 防 iOS 缩放；`prefers-reduced-motion` 降级。

### 离线版
- 重写为与主站同款设计 + 同一份引擎，离线同样支持深浅色 / 导航 / 键盘 / 滑动 / 进度恢复 / 重做错题。

### 修复
- 修复导航遮罩层（`.scrim`）关闭时仍铺满全屏、拦截点击导致整页无法操作的问题（未激活时 `pointer-events:none`）。
- 清理深色主题中重复的 `--surface` 声明。
